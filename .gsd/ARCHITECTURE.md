# Architecture

> Auto-generated by /map on 2026-01-19

## Overview

High-performance desktop application built with Tauri (v2), integrating a React frontend with a Rust backend workspace designed for scraping and automation tasks. The system utilizes Supabase for data persistence and authentication.

```mermaid
graph TD
    User[User] --> Frontend[React Frontend]
    Frontend -->|Tauri Commands| AppDesktop[app-desktop (Rust)]
    AppDesktop --> SharedLib[shared-lib]
    AppDesktop --> ScraperBot[scraper-bot]
    ScraperBot -->|Headless Chrome| Web[Target Websites]
    Frontend -->|Supabase SDK| DB[(Supabase)]
    AppDesktop -->|SQL/HTTP| DB
```

## Components

### Frontend
- **Purpose**: User interface for interaction, configuration, and data display.
- **Location**: `src/`
- **Tech**: React, Vite, TailwindCSS, Radix UI, TanStack Query.
- **Key Directories**:
  - `src/components`: UI components (Radix primitives).
  - `src/hooks`: Custom React hooks.
  - `src/integration`: External service integrations.

### Backend Workspace (`src-tauri`)

#### `app-desktop`
- **Purpose**: Main Tauri application entry point and command handlers.
- **Location**: `src-tauri/app-desktop`
- **Dependencies**: `tauri`, `tokio`, `serde`, `shared-lib`.

#### `scraper-bot`
- **Purpose**: Autonomous scraping or bot execution logic.
- **Location**: `src-tauri/scraper-bot`
- **Dependencies**: Likely `headless_chrome`, `reqwest` (via `shared-lib` or direct).

#### `shared-lib`
- **Purpose**: Core business logic and shared utilities.
- **Location**: `src-tauri/shared-lib`
- **Capabilities**: Database access (`rusqlite`), HTTP requests (`reqwest`), Browser automation (`headless_chrome`), Data parsing (`scraper`).

## Data Flow

1. **User Action**: User triggers a task via React UI.
2. **Command Dispatch**: Frontend invokes Tauri command.
3. **Execution**: Rust backend (`app-desktop`) receives command, delegates to `scraper-bot` or uses `shared-lib`.
4. **External Interaction**: Bot interacts with target websites or APIs.
5. **Persistence**: Data saved to local SQLite (via `rusqlite`) or synced to Supabase.
6. **Feedback**: Status updates pushed back to Frontend via events.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Supabase | External | Auth, Database, Realtime updates |
| Target Websites | External | Scraping targets |
| Local SQLite | Internal | Local caching/storage (via rusqlite) |

## Technical Debt

- [ ] `src/scraper-bot` exists in frontend source but usage is unclear (possibly abandoned or shared types?).
- [ ] `shared-lib` carries heavy dependencies (`headless_chrome`); consider if this should be isolated to `scraper-bot`.
- [ ] Ensure `src-tauri/scraper-bot` is properly integrated into the workspace and build pipeline.

## Conventions

**Structure**:
- Monorepo-style with separate frontend and Rust workspace.
- Rust code split into functional crates (`app`, `bot`, `shared`).
